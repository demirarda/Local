/**
 * @deprecated v2 §7 SERIES — stub emekli oldu, tam ürün seriesService.js.
 * Eski import yollarını kırmamak için ince re-export.
 */
export {
  generateSeriesInstances,
  generateSeriesInstances as generateRecurringInstances,
  spawnSeriesInstance,
} from './seriesService.js';

export { default } from './seriesService.js';
